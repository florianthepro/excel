# Dynamische Listensteuerung über den Namensmanager

<details>
<summary>Deutsche Excel-Version</summary>

```excel
=WENN(ODER($C2=$B$2;$C2=$B$7;$C2=$B$9);$C:$C;$D:$D)
```

</details>

<details>
<summary>Englische Excel-Version</summary>

```excel
=IF(OR($C2=$B$2,$C2=$B$7,$C2=$B$9),$C:$C,$D:$D)
```

</details>

<details>
<summary>Technischer Aufbau</summary>

`$C2`  Prüfzelle. Dieser Wert entscheidet, welche Liste verwendet wird.<br />
`$B$2`  Erster Vergleichswert. Wenn `$C2` gleich `$B$2` ist, wird Spalte `C` verwendet.<br />
`$C2`  Prüfzelle wird erneut verwendet.<br/>
`$B$7`  Zweiter Vergleichswert. Wenn `$C2` gleich `$B$7` ist, wird Spalte `C` verwendet.<br/>
`$C2`  Prüfzelle wird erneut verwendet.<br/>
`$B$9`  Dritter Vergleichswert. Wenn `$C2` gleich `$B$9` ist, wird Spalte `C` verwendet.<br/>
`$C:$C`  Rückgabebereich bei Treffer. Diese Spalte wird als Dropdown-Liste verwendet, wenn eine Bedingung zutrifft.<br/>
`$D:$D`  Rückgabebereich ohne Treffer. Diese Spalte wird als Dropdown-Liste verwendet, wenn keine Bedingung zutrifft.

</details>

**Konfigurieren:**

Namensmanager:
<table>
<tr><td>Wo:</td><td>Formeln → Namensmanager → Neu</td></tr>
<tr><td>Name:</td><td><code>&lt;Listen Name&gt;</code></td></tr>
<tr><td>Bezieht sich auf:</td><td><code>&lt;Formel einfügen&gt;</code></td></tr>
</table>

Datenüberprüfung:
<table>
<tr><td>Wo:</td><td>Daten → Datenüberprüfung</td></tr>
<tr><td>Typ:</td><td>Liste</td></tr>
<tr><td>Quelle:</td><td>=<code>&lt;Listen Name&gt;</code></td></tr>
</table>
