# Dynamische Listensteuerung über den Namensmanager

## Basisformel

### Deutsche Excel-Version

```excel
=WENN(ODER($C2=$B$2;$C2=$B$7;$C2=$B$9);$C:$C;$D:$D)
```

### Englische Excel-Version

```excel
=IF(OR($C2=$B$2,$C2=$B$7,$C2=$B$9),$C:$C,$D:$D)
```

---

# Technischer Aufbau

## Prüfbedingung

### Deutsch

```excel
ODER($C2=$B$2;$C2=$B$7;$C2=$B$9)
```

### Englisch

```excel
OR($C2=$B$2,$C2=$B$7,$C2=$B$9)
```

Prüft, ob `C2` einem der definierten Referenzwerte entspricht.

---

## Rückgabebereich "WAHR"

```excel
$C:$C
```

Wird verwendet, wenn mindestens eine Bedingung erfüllt ist.

---

## Rückgabebereich "FALSCH"

```excel
$D:$D
```

Wird verwendet, wenn keine Bedingung erfüllt ist.

---

## Gesamtablauf

```text
1. Prüfe C2
2. Vergleiche mit B2, B7, B9
3. Treffer → Spalte C
4. Kein Treffer → Spalte D
```

---

# Skalierbares Konzept

Weitere Vergleichswerte können einfach ergänzt werden.

### Deutsche Excel-Version

```excel
=WENN(ODER($C2=$B$2;$C2=$B$7;$C2=$B$9;$C2=$B$12;$C2=$B$15);$C:$C;$D:$D)
```

### Englische Excel-Version

```excel
=IF(OR($C2=$B$2,$C2=$B$7,$C2=$B$9,$C2=$B$12,$C2=$B$15),$C:$C,$D:$D)
```

---

# Erweiterung auf weitere Listen

## Liste C oder D

```text
Treffer → C:C
Sonst   → D:D
```

## Liste C, D oder E

### Deutsch

```excel
=WENN($C2=$B$2;$C:$C;WENN($C2=$B$7;$D:$D;$E:$E))
```

### Englisch

```excel
=IF($C2=$B$2,$C:$C,IF($C2=$B$7,$D:$D,$E:$E))
```

---

# Verwendung in der Datenüberprüfung

## 1. Namen anlegen

```text
Formeln → Namensmanager → Neu
```

Name:

```text
MeineListe
```

Bezieht sich auf:

### Deutsch

```excel
=WENN(ODER($C2=$B$2;$C2=$B$7;$C2=$B$9);$C:$C;$D:$D)
```

### Englisch

```excel
=IF(OR($C2=$B$2,$C2=$B$7,$C2=$B$9),$C:$C,$D:$D)
```

---

## 2. Datenüberprüfung konfigurieren

```text
Daten → Datenüberprüfung
```

Typ:

```text
Liste
```

Quelle:

```excel
=MeineListe
```

---

## Best Practice

Die Formel sollte nur die Logik enthalten:

```text
Bedingung → Bereich A
Sonst     → Bereich B
```

Dadurch lassen sich später weitere Bedingungen oder zusätzliche Listen ohne Änderung der Datenüberprüfung erweitern.
