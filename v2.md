# Dynamische Listensteuerung über den Namensmanager

## Deutsche Excel-Version

```excel
=WENN(ODER($C2=$B$2;$C2=$B$7;$C2=$B$9);$C:$C;$D:$D)
```

## Englische Excel-Version

```excel
=IF(OR($C2=$B$2,$C2=$B$7,$C2=$B$9),$C:$C,$D:$D)
```

---

## Technischer Aufbau

`$C2`  Prüfzelle. Dieser Wert entscheidet, welche Liste verwendet wird.\n
`$B$2`  Erster Vergleichswert. Wenn `$C2` gleich `$B$2` ist, wird Spalte `C` verwendet.
`$C2`  Prüfzelle wird erneut verwendet.
`$B$7`  Zweiter Vergleichswert. Wenn `$C2` gleich `$B$7` ist, wird Spalte `C` verwendet.
`$C2`  Prüfzelle wird erneut verwendet.
`$B$9`  Dritter Vergleichswert. Wenn `$C2` gleich `$B$9` ist, wird Spalte `C` verwendet.
`$C:$C`  Rückgabebereich bei Treffer. Diese Spalte wird als Dropdown-Liste verwendet, wenn eine Bedingung zutrifft.
`$D:$D`  Rückgabebereich ohne Treffer. Diese Spalte wird als Dropdown-Liste verwendet, wenn keine Bedingung zutrifft.

---

## Logik

```text
Wenn C2 gleich B2, B7 oder B9 ist:
    Dropdown = Spalte C

Sonst:
    Dropdown = Spalte D
```

---

## Skalierbares Konzept

Weitere Bedingungen können einfach ergänzt werden.

### Deutsche Excel-Version

```excel
=WENN(ODER($C2=$B$2;$C2=$B$7;$C2=$B$9;$C2=$B$12;$C2=$B$15);$C:$C;$D:$D)
```

### Englische Excel-Version

```excel
=IF(OR($C2=$B$2,$C2=$B$7,$C2=$B$9,$C2=$B$12,$C2=$B$15),$C:$C,$D:$D)
```

### Technisches Prinzip

`$C2`  
Bleibt immer die Prüfzelle.

`$B$2`, `$B$7`, `$B$9`, `$B$12`, `$B$15`  
Sind die erlaubten Vergleichswerte.

`$C:$C`  
Liste bei Treffer.

`$D:$D`  
Liste ohne Treffer.

---

## 2. Datenüberprüfung konfigurieren

```text
Daten → Datenüberprüfung
```

Typ:

```text
Liste
```

Quelle:

```excel
=MeineListe
```

---

## Namensmanager

```text
Formeln → Namensmanager → Neu
```

Name:

```text
MeineListe
```

Bezieht sich auf:

### Deutsch

```excel
=WENN(ODER($C2=$B$2;$C2=$B$7;$C2=$B$9);$C:$C;$D:$D)
```

### Englisch

```excel
=IF(OR($C2=$B$2,$C2=$B$7,$C2=$B$9),$C:$C,$D:$D)
```

---

## Wichtig

`$C2` bedeutet:

```text
Spalte C ist fixiert.
Zeile 2 ist relativ.
```

Dadurch kann die Formel zeilenweise funktionieren:

```text
Zeile 2 → prüft C2
Zeile 3 → prüft C3
Zeile 4 → prüft C4
```

Die Datenüberprüfung bleibt dabei immer gleich:

```excel
=MeineListe
```
