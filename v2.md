# Dynamische Listensteuerung über den Namensmanager

## Deutsche Excel-Version

```excel
=WENN(ODER($C2=$B$2;$C2=$B$7;$C2=$B$9);$C:$C;$D:$D)
```

## Englische Excel-Version

```excel
=IF(OR($C2=$B$2,$C2=$B$7,$C2=$B$9),$C:$C,$D:$D)
```

---

## Technischer Aufbau

`$C2`  Prüfzelle. Dieser Wert entscheidet, welche Liste verwendet wird.<br />
`$B$2`  Erster Vergleichswert. Wenn `$C2` gleich `$B$2` ist, wird Spalte `C` verwendet.<br />
`$C2`  Prüfzelle wird erneut verwendet.<br />
`$B$7`  Zweiter Vergleichswert. Wenn `$C2` gleich `$B$7` ist, wird Spalte `C` verwendet.<br />
`$C2`  Prüfzelle wird erneut verwendet.<br />
`$B$9`  Dritter Vergleichswert. Wenn `$C2` gleich `$B$9` ist, wird Spalte `C` verwendet.<br />
`$C:$C`  Rückgabebereich bei Treffer. Diese Spalte wird als Dropdown-Liste verwendet, wenn eine Bedingung zutrifft.<br />
`$D:$D`  Rückgabebereich ohne Treffer. Diese Spalte wird als Dropdown-Liste verwendet, wenn keine Bedingung zutrifft.<br />

## 2. Datenüberprüfung konfigurieren

```text
Daten → Datenüberprüfung
```

Typ:

```text
Liste
```

Quelle:

```excel
=MeineListe
```

---

## Namensmanager

```text
Formeln → Namensmanager → Neu
```

Name:

```text
MeineListe
```

Bezieht sich auf:

### Deutsch

```excel
=WENN(ODER($C2=$B$2;$C2=$B$7;$C2=$B$9);$C:$C;$D:$D)
```

### Englisch

```excel
=IF(OR($C2=$B$2,$C2=$B$7,$C2=$B$9),$C:$C,$D:$D)
```

---

## Wichtig

`$C2` bedeutet:

```text
Spalte C ist fixiert.
Zeile 2 ist relativ.
```

Dadurch kann die Formel zeilenweise funktionieren:

```text
Zeile 2 → prüft C2
Zeile 3 → prüft C3
Zeile 4 → prüft C4
```

Die Datenüberprüfung bleibt dabei immer gleich:

```excel
=MeineListe
```
